<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Black.NoTitleBar.Fullscreen">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#FFFFFFFF</item>
        <item name="colorPrimaryDark">#000000</item>
        <item name="colorAccent">#ffc70000</item>
        <item name="android:colorPrimary">@color/legacy_primary</item>
        <item name="android:colorPrimaryDark">@color/legacy_primary_dark</item>
        <item name="android:textColorPrimary">@color/legacy_primary</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
